import { CreateElement, VNode } from 'vue';

export default {
  methods: {
    /**
     * useRowDragSortRow
     * @param h
     * @param trVNode
     */
    useRowDragSortRow(h: CreateElement, trVNode: VNode): VNode | null {
      return null;
    },
  },
};
